// DEPRECATED: This script generated synthetic domain content and fake repository URLs.
// It is intentionally disabled. Use the real GitHub discovery engine from
// /admin/discovery or /api/admin/project-discovery instead.

console.error([
  'scripts/populate-domain-content.js is deprecated and disabled.',
  'It generated fake projects and must not be used for production data.',
  'Use the real GitHub discovery engine in /admin/discovery instead.'
].join('\n'));

process.exit(1);
