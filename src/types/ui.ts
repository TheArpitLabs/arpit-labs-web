export interface SectionWrapperProps {
  title: string;
  subtitle: string;
  badge?: string;
}

export interface CTAProps {
  title: string;
  description: string;
  primaryText: string;
  secondaryText?: string;
}
